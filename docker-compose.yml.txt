
services:
  cheshire-psyche:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cheshire-psyche-core
    restart: always
    environment:
      - PORT=3005
      - NODE_ENV=production
      - API_KEY=${API_KEY}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - VECTOR_PROXY_URL=${VECTOR_PROXY_URL}
      - DB_DIRECT_HOST=${DB_DIRECT_HOST}
      - DB_DIRECT_PORT=${DB_DIRECT_PORT}
      - DB_USER=${DB_USER}
      - DB_PASS=${DB_PASS}
    ports:
      - "80:3005"
    deploy:
      resources:
        limits:
          memory: 2G
    networks:
      - cheshire_net

networks:
  cheshire_net:
    driver: bridge
